One line between top level declarations.

Exception: files should when useful be separated into sections, separated by two empty lines and an eye-catching header comment, eg:

```
def bar(): ...


# ---------- SECTION NAME IN ALL CAPS ----------

def foo(): ...
```

Note the double empty lines before the section header comment.
