* tasks

** stop injecting "hasTrustDialogAccepted": true, probably

now that state is persisted, I should see this dialog once per project instead of every time I start a claudebox, right? if so, that's good.

** re-use an existing container if it matches project?

is there any point isolating different claudes on the same project in different claudebox containers if they share their claude config dir? if not, we should probably either go fully-temporary on containers (destroy them at end of session by default) or fully shared & persistent (keep the container around until project destroyed or explicitly pruned, and re-use that container for every `claudebox` command in the same project).

** convert claudebox, buildbox into one script with subcommands

claudebox run
claudebox start ?? expose difference b/w container run/start?
claudebox build
claudebox prune
claudebox du

Might be a good time to convert it from sh to Python for maintainability's sake.

** some sort of preservation of context

Claude Code preserves some state when you re-open claude in the same directory ("project"): memories, for instance. I'd similarly like to persist some state on a per-directory/project basis in Claudebox. In particular:

TODO DESIGN

** allow giving github url as argument and use that instead of cwd

given a github url, clone the git repo. where, though?
- temp directory?
- clone it into current dir using its name?
- make a directory specifically for git clones?
- have a config file for this script that specifies?

** build a convenient sandbox-exec command-line wrapper
PR #6 open: bin/devbox + DEVBOX.md, written but never run on macOS.
name (maybe): devbox?
purpose: allow me to interact with a repo on macOS with less trust.

example threats I'd like to defend against, at least a little:
- malicious git hooks etc authored by prompt-injected LLM
- malicious dependency/supply chain attack
- honest mistakes in LLM- or human-written code

defaults:

- read/write access to cwd.

- no network access. want a flag (--network=yes, --network=no) to control this.

- read access to some reasonable selection of system dirs so I can run commands.

- read access to some reasonable subset of my homedir so eg `git` still works; my current
  ~/.claude/settings.json sandbox block might be helpful as a starting point:

    "filesystem": {
      "denyRead": [
        "~/",
        "/Users/"
      ],
      "allowRead": [
        "~/.gitconfig",
        "~/.gitexclude",
        "~/.rustup/",
        "~/.cargo/"
      ]

problem:
- what about git push/pull? needs network. if I give it network, how do I defend against
  exfiltration of eg github credentials? seems hard. solve this later; any sandbox is an
  improvement over no sandbox.

* future ideas

** consider converting claudebox to python if it gets more complex

** container lifetime defaults

now: claudebox runs a long-lived container.
alternatives:

- `container run --rm` so it's transient.

- `container start` so it doesn't sleep when I leave the session. I often find myself
  "exec bash"-ing in to the container. If I unexpectedly ctrl-D out of the `container run`
  session, the container stops and I lose state.

- cache/reuse containers according to the working directory.

** github permissions convenience

would be nice to have a better way to grant tightly scoped github permissions than manually generating PATs.
- make a GH app?
- other options?
